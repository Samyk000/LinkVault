# Environment Variables Example
# Copy this file to .env.local and fill in your actual values

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: Service role key for admin operations (keep this secret)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Debug Configuration
# Set to 'true' to enable debug dashboard (development only)
# DEBUG_MODE=false

# Environment (automatically set by deployment platform)
# NODE_ENV=development  # Uncomment for local development
# NODE_ENV=production   # Set by deployment platforms