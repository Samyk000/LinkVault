# Production Environment Configuration
# This file ensures debug dashboard is disabled in production

# Set environment to production
NODE_ENV=production

# Explicitly disable debug mode (uncomment to enable debug dashboard in production for troubleshooting)
# DEBUG_MODE=false

# Supabase Configuration (use production credentials)
NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}